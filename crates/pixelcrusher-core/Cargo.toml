[package]
name = "pixelcrusher-core"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0"
image = { version = "0.25", default-features = false, features = ["png", "jpeg", "gif", "ico"] }
serde = { version = "1.0", features = ["derive"] }
thiserror = "2.0"
which = "8.0"

[dev-dependencies]
tempfile = "3.23"
